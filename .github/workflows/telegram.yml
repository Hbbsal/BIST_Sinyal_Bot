name: BIST Günlük Analiz

on:
  schedule:
    - cron: '15 7 * * 1-5'   # UTC 07:15 = Türkiye saatiyle 10:15
  push:
    paths:
      - "stocks.txt"
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      # 🚀 Node.js 24 uyumluluğu için v3'ten v4'e yükseltildi
      - name: Repo'yu çek
        uses: actions/checkout@v4

      - name: Önceki stocks.txt dosyasını al
        continue-on-error: true
        run: |
          git fetch origin main --depth=2
          git checkout HEAD^1 -- stocks.txt || echo "" > old_stocks.txt
          mv stocks.txt new_stocks.txt
          # Eğer git checkout başarısız olup boş dosya ürettiyse isimlendirmeyi garantiye alıyoruz
          if [ ! -f old_stocks.txt ]; then mv stocks.txt old_stocks.txt; fi

      - name: Farklı hisseleri bul
        run: |
          comm -23 <(sort old_stocks.txt) <(sort new_stocks.txt) > removed.txt
          comm -13 <(sort old_stocks.txt) <(sort new_stocks.txt) > added.txt
          echo "ADDED=$(cat added.txt | tr '\n' ',' | sed 's/,$//')" >> $GITHUB_ENV
          echo "REMOVED=$(cat removed.txt | tr '\n' ',' | sed 's/,$//')" >> $GITHUB_ENV

      # 🚀 Node.js 24 uyumluluğu için v4'ten v5'e yükseltildi
      - name: Python kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Bağımlılıkları yükle
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas python-telegram-bot pillow

      - name: Botu çalıştır (değişen hisseler)
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          ADDED: ${{ env.ADDED }}
          REMOVED: ${{ env.REMOVED }}
        run: |
          # Python kodunun yeni stocks.txt (yani new_stocks.txt) dosyasını okuyabilmesi için ismi geri çeviriyoruz
          cp new_stocks.txt stocks.txt
          python bist_analiz.py

      - name: Başarılı Çalışma Bildirimi
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python - <<EOF
          from telegram import Bot
          import os
          token = os.getenv("TELEGRAM_TOKEN")
          chat_id = os.getenv("TELEGRAM_CHAT_ID")
          bot = Bot(token=token)
          bot.send_message(chat_id=chat_id, text="✅ Workflow başarıyla çalıştı: Güncel hisselerden rapor gönderildi!")
          EOF
